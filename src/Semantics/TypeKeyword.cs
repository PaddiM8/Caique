namespace Caique.Semantics
{
    public enum TypeKeyword
    {
        i8, i32, i64,
        f8, f32, f64,
        NumberLiteral,
        Bool,
        Void,
        Identifier,
        Unknown,
    }
}