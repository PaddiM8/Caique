module LibcUnsafe
{
    #ffi
    stdout usize;

    #ffi
    func printf(pattern [u8], arg [u8]);

    #ffi
    func fputs(str [u8], stream usize);

    #ffi
    func malloc(size usize) usize;

    func GetStdout() usize
    {
        LibcUnsafe:stdout
    }
}
