module LibcUnsafe
{
    #ffi
    pub let stdout usize;

    #ffi
    pub func printf(pattern [u8], arg [u8]);

    #ffi
    pub func fputs(str [u8], stream usize);

    #ffi
    pub func malloc(size usize) usize;
}
