class LibcUnsafe
{
    #ffi
    static stdout usize;

    #ffi
    static func printf(pattern [u8], arg [u8]);

    #ffi
    static func fputs(str [u8], stream usize);

    static func GetStdout() usize
    {
        LibcUnsafe:stdout
    }
}
